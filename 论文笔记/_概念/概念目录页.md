---
tags: [MOC, concept-index]
---

# 📖 概念目录页

> 概念笔记总索引。按知识领域分组，每个条目包含定义摘要。

**共 124 条概念** · 生成时间: 2026-07-16 11:31

## 📥 待分类

- **[[Coq]]**（Rocq / Coq 8.20）  
  Rocq（原名 Coq，2025 年更名）是一个交互式定理证明器，基于归纳构造演算（Calculus of Inductive Constructions），支持形式化证明的开发和验证。
- **[[Isabelle]]**（Isabelle/HOL）  
  Isabelle 是一个通用的交互式定理证明框架，Isabelle/HOL 是其最流行的对象逻辑，基于高阶逻辑（Higher-Order Logic）进行形式化证明。
- **[[Lean 4]]**（Lean4 / Lean）  
  Lean 4 是一个交互式定理证明器（proof assistant）和函数式编程语言，用于形式化数学证明，是 Lean 定理证明器的最新主要版本。
- **[[统一训练范式]]**（Unified Training Paradigm / 统一训练框架）  
  统一训练范式是 DeepSeekMath 提出的用于理解 SFT、RFT、DPO、PPO、GRPO 等不同训练方法的通用数学框架：所有方法的梯度都可以表示为 $\nabla_\theta \mathcal{J} = \mathbb{E}_{(q,o) \sim \mathcal{D}}[\frac{1}{|o|}\sum_t \text{GC} \cdot …

## 📦 数据集

- **[[Common Crawl]]**（CommonCrawl / CC）  
  Common Crawl 是一个大规模网络爬虫语料库，包含数十亿网页的原始 HTML 数据、元数据和文本提取，是训练大型语言模型最常用的开源预训练数据源之一。
- **[[DAPO-Math-17K]]**  
  DAPO 论文发布的开源数学推理训练数据集，包含约 17K 数学问题，广泛用于 LLM reasoning RL 训练。
- **[[DeepSeekMath Corpus]]**（DeepSeekMath语料库）  
  DeepSeekMath Corpus 是 DeepSeekMath 团队从 Common Crawl 中通过迭代式数据筛选 pipeline 提取的高质量数学预训练语料库，共计 120B tokens，涵盖多语言（以英文和中文为主）数学内容。
- **[[OpenWebMath]]**  
  OpenWebMath 是一个高质量数学网页文本数据集，从 Common Crawl 中筛选产生，共计 13.6B tokens。它被 DeepSeekMath 用作数据收集 pipeline 的种子语料。

## 🔬 深度学习基础

- **[[Birkhoff Polytope]]**（双随机矩阵流形）  
  所有 $n \times n$ 双随机矩阵（每行每列和均为 1 且所有元素非负）构成的凸多面体集合，是置换矩阵多面体的凸包。
- **[[Entropy]]**（信息熵 / Shannon Entropy / Token Entropy）  
  信息论中度量不确定性的核心概念。在 LLM 中，token 级熵 $H(p) = -\sum_i p_i \log p_i$ 反映模型在当前位置的预测不确定性——低熵表示模型"确信"，高熵表示模型"犹豫"。
- **[[KL散度]]**（KL Divergence / Kullback-Leibler Divergence / 相对熵）  
  KL 散度（Kullback-Leibler Divergence）是衡量两个概率分布之间差异的非对称度量，定义为 $D_{KL}(P||Q) = \sum_x P(x) \log \frac{P(x)}{Q(x)}$。
- **[[Newton-Schulz Iterations]]**（Newton-Schulz迭代 / NS迭代）  
  一种通过迭代矩阵乘法逼近矩阵正交化的数值方法，将任意矩阵逐步变换为其奇异值分解中的正交因子 $\mathbf{U}\mathbf{V}^\top$。
- **[[Sinkhorn-Knopp Algorithm]]**（SK算法 / Sinkhorn迭代）  
  一种通过交替行列归一化将非负矩阵投影到双随机矩阵（[[Birkhoff Polytope]]）流形的迭代算法。
- **[[fastText]]**  
  fastText 是 Facebook AI Research 开发的高效文本分类和词向量训练库，使用词袋模型和 n-gram 特征进行快速文本分类，适合大规模文本分类任务。
- **[[累积对数概率]]**（Cumulative Log-Probability / Log-Probability Scoring）  
  对序列生成过程中每个 token 的对数条件概率求和，作为该序列质量的评分指标。

## 🎯 强化学习

### RL 训练专题

- **[[Agon]]**  
  Agon（希腊语 agon，意为"竞赛/较量"）是一种跨模型竞争强化学习框架，由 Vladislav Beliaev 于 2026 年提出。核心思想是让**两个能力相当但行为不同的模型在 RL 训练中互为隐式评分器**，通过奖励"击败对手"来隐式评判推理质量，无需 process reward model 或人工标注。
- **[[Cross-Model RL]]**  
  跨模型强化学习（Cross-Model RL）是一种 RL 训练范式，在训练过程中使用**两个或多个不同的模型/策略相互交互**来产生训练信号，而非单一模型在自己的 rollout 上进行自我改进。Agon 是这一范式的代表性实例。
- **[[Draft-and-Challenge]]**  
  Draft-and-Challenge 是 Agon 框架的核心训练协议，一种**不对称、角色轮换的双模型交互范式**。每一步中，一个模型起草解答（draft），另一个模型阅读其摘要并尝试超越（challenge）。
- **[[Positive-Credit Contamination]]**（正向信用污染）  
  GRPO 风格 RLVR 中的一种关键失效模式：正确轨迹中低概率的不可靠尾 token 由于 uniform credit assignment 获得与合理 token 相同的正向 advantage，导致错误行为被不自觉强化。
- **[[Tail-Risk Score]]**（尾风险评分）  
  TACO 中用于衡量每个生成 token 属于不可靠尾分布的风险评分，基于 token 的 surprisal 与局部熵期望 surprisal 的偏差。
### 算法基础

- **[[Credit Assignment]]**（信用分配）  
  强化学习中确定每个动作/决策对最终奖励贡献程度的过程，决定了哪些行为应该被强化或抑制。
- **[[DIS]]**（Direct Double-Sided Importance Sampling / 直接双边重要性采样）  
  DIS (Direct Double-Sided Importance Sampling) 是 SAO 中的稳定化机制，使用严格的双边 token 级重要性采样裁剪，将 importance ratio 超出预设区间的 token 从梯度计算中完全屏蔽。
- **[[GAE]]**
- **[[Importance Sampling]]**（重要性采样 / IS / importance weighting）  
  重要性采样 (Importance Sampling) 是一种统计技术，用于在数据来自分布 $\mu$ 而非目标分布 $\pi$ 时，通过加权修正来无偏估计 $\pi$ 下的期望。
- **[[Off-Policy]]**（off-policy learning / 离策略学习 / policy lag）  
  Off-Policy (离策略学习) 指训练数据由与当前策略不同的行为策略生成的情况。在异步 RL 中，由于 rollout 生成和模型更新之间存在时间差，行为策略 (rollout policy) 通常滞后于当前训练策略，产生 policy lag 和 off-policy effects。
- **[[PPO]]**（Proximal Policy Optimization）  
  一种基于策略梯度的强化学习算法，通过 clipped surrogate objective 限制每次更新的步长，防止策略突变导致训练崩溃。
- **[[Self-Play]]**  
  Self-Play 是一种训练范式，模型通过与自身的历史版本或变体进行博弈来改进。源自 AlphaGo/AlphaZero 等经典 RL 方法，近年来被应用于 LLM 的推理能力提升。
- **[[TRPO]]**
### LLM RL 算法

- **[[DAPO]]**（Decoupled Clip and Dynamic Sampling Policy Optimization）  
  一种 GRPO 变体，通过解耦 clip 机制和动态采样策略来改进 GRPO 训练稳定性和效率。
- **[[GRPL]]**（Group Relative Preference Learning / 分组相对偏好学习）  
  Group Relative Preference Learning (GRPL) 是一种基于 [[GRPO]] 的强化学习训练方法，专门用于训练[[Reward Model|奖励模型]]遵循自然语言原则进行评估。与传统[[RLHF]]中学习隐式偏好不同，GRPL 将奖励模型视为策略 $\pi_\theta$，通过强化学习优化其生成符合给定原则的评估输出的能…
- **[[GRPO]]**（Group Relative Policy Optimization / 组相对策略优化）  
  GRPO (Group Relative Policy Optimization) 是一种无需价值模型的强化学习算法，通过为每条 prompt 采样多条 response 并在组内计算标准化相对优势来优化策略。被 DeepSeek-R1、Qwen3、GLM-5 等主流 LLM 广泛使用。
- **[[Group Relative Policy Optimization]]**（GRPO / 组相对策略优化）  
  一种强化学习策略优化算法，通过在同一 prompt 下采样一组响应并计算组内相对优势来优化策略，无需独立的 critic/value 网络。
- **[[SAO]]**（Single-Rollout Asynchronous Optimization / 单次部署异步优化）  
  SAO (Single-Rollout Asynchronous Optimization) 是一种面向 LLM agentic post-training 的异步强化学习算法，核心是用单次 rollout 采样替代 GRPO 的组采样，配合双边重要性采样裁剪 (DIS) 和价值模型实现稳定的异步训练。
- **[[STAPO]]**  
  一种梯度感知的 token-level 优化稳定方法，通过抑制梯度幅值过大的 token 更新来增强 GRPO 训练稳定性。
- **[[TACO]]**（Tail-Aware Credit CalibratiOn）  
  一种针对 GRPO 风格 RLVR 的 token-level credit 校准方法，通过尾风险评分软性抑制不可靠低概率 token 的正向 advantage，解决 Positive-Credit Contamination 问题。
### 对齐与奖励

- **[[Direct Preference Optimization]]**（DPO / 直接偏好优化）  
  DPO 是一种无需显式训练奖励模型的偏好优化方法，直接利用偏好数据对策略模型进行优化，将 RLHF 的奖励建模和策略优化两个阶段合并为一个过程。
- **[[Generative Reward Model]]**（GRM / 生成式奖励模型）  
  一种利用生成式大模型本身作为奖励模型的范式，模型直接在自然语言中评估响应质量，而非输出标量分数，将评判能力与生成能力统一。
- **[[RLHF]]**
- **[[RLVR]]**（RL with Verifiable Rewards / 可验证奖励强化学习 / Outcome Reward RL）  
  RLVR (Reinforcement Learning with Verifiable Rewards) 是一种利用**可自动验证的二元/标量奖励信号**进行强化学习后训练的范式。与依赖学习型奖励模型 (Reward Model, RM) 的 RLHF 不同，RLVR 使用确定性、无噪声的验证器信号。
### 其他

- **[[MoE]]**

## 🏗️ 大模型架构

### 其他

- **[[Anticipatory Routing]]**（预判路由 / 预期路由）  
  一种 MoE 训练稳定性技术，通过解耦骨干网络和路由网络的同步更新来打破路由引起的异常值恶性循环：当前步使用历史参数的路由决策，而非同步更新。
- **[[Auxiliary-Loss-Free Load Balancing]]**（Auxiliary-Loss-Free / 无辅助损失负载均衡）  
  一种 MoE 负载均衡策略，不通过辅助损失项影响训练目标，而是通过动态调整路由 bias 来平衡专家负载。
- **[[Hash Routing]]**（哈希路由）  
  一种 MoE 路由策略，根据输入 token ID 通过预定义的哈希函数确定目标专家，而非使用可学习路由网络，完全避免了路由计算和负载均衡问题。
- **[[Manifold-Constrained Hyper-Connections]]**（mHC / 流形约束超连接）  
  对标准 Hyper-Connections (HC) 的改进，将残差映射矩阵约束到双随机矩阵流形（[[Birkhoff Polytope]]）上，增强深层网络信号传播的数值稳定性。
### 注意力机制

- **[[Attention Sink]]**（注意力汇）  
  一种注意力机制技巧，为每个 attention head 引入可学习的"汇"（sink）logit，使 query 可以选择不关注任何 token（总 attention score < 1），从而减少对无关 token 的强制注意力分配。
- **[[Block Sparse Attention]]**（Naive BSA / 朴素块稀疏注意力）  
  一种理想化的块稀疏注意力方法，通过计算全注意力获取精确的块质量（chunk mass），选择质量最高的 top-K 块进行稀疏注意力计算。
- **[[Chunk-wise Sparse Attention]]**（块级稀疏注意力 / 块稀疏注意力）  
  将输入序列划分为等大小的块（chunk），通过选择性关注部分块来实现线性或次二次复杂度的注意力机制。
- **[[Compressed Sparse Attention]]**（CSA / 压缩稀疏注意力）  
  一种混合注意力架构，先沿序列维度压缩 KV Cache，再在压缩后的 KV entries 上执行稀疏注意力，大幅降低长序列推理的 FLOPs 和 KV Cache 大小。
- **[[DSA]]**（DeepSeek Sparse Attention）  
  一种基于动态内容感知的 token 级稀疏注意力机制，通过轻量级 Indexer 打分 + Top-k 选择，将传统密集注意力的 O(L²) 计算复杂度降至 O(L·k)，在大幅降低计算/显存开销的同时保持长上下文性能近乎无损。
- **[[DeepSeek Sparse Attention]]**（DSA / DeepSeek稀疏注意力）  
  DeepSeek 提出的稀疏注意力方案，通过索引器选择 top-k 压缩 KV entries 进行注意力计算，而非对所有 tokens 进行稠密注意力，大幅减少长序列注意力计算量。
- **[[GQA]]**（Grouped-Query Attention / 分组查询注意力）  
  一种介于多头注意力（MHA）和多查询注意力（MQA）之间的注意力变体，多个查询头共享同一个 Key-Value 头组，以减少 KV 缓存大小和加速推理。
- **[[Heavily Compressed Attention]]**（HCA / 重度压缩注意力）  
  一种高效的注意力架构，对 KV Cache 进行更高压缩比（$l_h \gg l_c$）的压缩，但不使用稀疏注意力，而是保持稠密注意力。
- **[[Hierarchical Softmax]]**（层次化 Softmax / 层次化归一化）  
  将标准 softmax 归一化解构为多级层次结构，逐步分配概率质量而非一次性全部分配。
- **[[HoPE]]**（混合位置编码 / Hybrid Positional Encoding）  
  一种混合位置编码方法，保留 [[RoPE]] 中旋转周期不超过预训练上下文长度的维度，将其余维度替换为 NoPE（无位置编码），同时提供长度外推和信息保留能力。
- **[[Landmark Token]]**（地标 Token / 块标记 Token）  
  一种添加到每个块末尾的特殊 token，其隐藏状态被用作该块的压缩摘要表示，用于块级别的路由和选择。
- **[[MLA]]**（Multi-Head Latent Attention / 多头潜注意力）  
  一种将注意力机制中的 Key 和 Value 投影到低秩潜空间 (latent space) 并进行压缩缓存的技术，在推理时动态重建完整 K、V 矩阵，从而大幅减少 KV Cache 显存占用。
- **[[Multi-Head Attention]]**（MHA / 多头注意力）  
  Multi-Head Attention (MHA) 是 Transformer 架构的核心组件，将输入投影到多个注意力头中并行计算注意力，然后将所有头的输出拼接并线性变换。
- **[[Multi-Query Attention]]**（MQA / 多查询注意力）  
  一种注意力机制的变体，所有 query head 共享同一组 key-value head，大幅减少 KV Cache 大小和注意力计算量。
- **[[Native Sparse Attention]]**（NSA / NATIVE SPARSE ATTENTION / 原生稀疏注意力）  
  一种硬件对齐的原生可训练稀疏注意力机制，通过设计专门的 GPU kernel 来支持动态稀疏注意力模式，同时保持端到端可训练性。
- **[[RingAttention]]**（Ring Attention / 环形注意力 / 块状 Transformer）  
  一种通过将输入序列分块并在多个设备间形成环形通信模式来实现近无限上下文长度的注意力机制，每个设备只存储并计算其分配的块。
- **[[RoPE]]**（Rotary Position Embedding / 旋转位置编码）  
  RoPE (Rotary Position Embedding) 是一种将位置信息编码到注意力机制中的方法，通过旋转矩阵对 Query 和 Key 向量进行变换，使得内积自然包含相对位置信息。
- **[[Rotary Positional Embedding]]**（RoPE / 旋转位置编码）  
  一种通过旋转变换将位置信息编码到 attention query/key 中的位置编码方法，自然地引入了相对位置信息。
### 模型架构组件

- **[[DeepSeek LLM]]**（DeepSeek LLM 系列）  
  DeepSeek LLM 是 DeepSeek-AI 开发的一系列开源大语言模型，采用标准的 Transformer 架构（使用 Multi-Head Attention、RoPE、SwiGLU、GQA 等组件），是 DeepSeekMath、DeepSeek-Coder 等衍生模型的基础。
- **[[DeepSeek-Coder]]**（DeepSeek Coder）  
  DeepSeek-Coder 是 DeepSeek-AI 开发的代码大语言模型系列，在大量代码数据上预训练，具有强大的编程能力。DeepSeek-Coder-Base-v1.5 7B 被用作 DeepSeekMath 的初始化模型。
- **[[DeepSeekMoE]]**（DeepSeek混合专家）  
  DeepSeek 系列模型的 MoE 架构，采用细粒度路由专家 + 共享专家的设计，通过更细粒度的专家划分实现更高的专家专业化程度。
- **[[Mixture-of-Experts]]**（MoE / 混合专家）  
  一种模型架构范式，将 FFN 层替换为多个"专家"子网络，通过路由机制为每个 token 选择激活部分专家，在参数量大幅增加的同时控制计算成本。
- **[[MoE]]**（Mixture of Experts / 混合专家模型）  
  一种将模型的前馈层 (FFN) 替换为多个独立的"专家"子网络，通过门控路由机制对每个 token 动态选择少量专家参与计算的稀疏模型架构，在增大总参数量的同时保持激活参数量和计算量相对稳定。
- **[[Qwen3]]**（Qwen3-0.6B / Qwen3-1.7B / Qwen3-4B）  
  阿里巴巴通义千问团队发布的开源大语言模型系列，支持 0.6B 到 8B 多个规模。[[TemperatureScaling]] 论文选择 Qwen3 系列作为主要实验模型，验证了多温度缩放在不同规模模型上的一致性收益。
- **[[SwiGLU]]**（Swish-Gated Linear Unit）  
  SwiGLU 是一种激活函数，结合了 Swish 激活函数和门控线性单元 (GLU)。它通过门控机制选择性地传递信息，已被证明在语言模型中优于标准 ReLU 或 GELU。
- **[[SwiGLU Clamping]]**（SwiGLU截断）  
  对 SwiGLU 激活函数的线性分量和门控分量分别施加数值截断，防止训练过程中产生极端异常值导致 loss spike。

## 🛡️ 对齐与安全

- **[[RLHF]]**（Reinforcement Learning from Human Feedback / 基于人类反馈的强化学习）  
  RLHF（Reinforcement Learning from Human Feedback）是一种通过人类偏好信号来微调语言模型的对齐方法，核心是用人类反馈训练奖励模型，再用强化学习优化策略模型。

## ⚡ 训练与优化

- **[[BetterTogether]]**（两者结合 / Fine-Tuning and Prompt Optimization）  
  BetterTogether 是一种将 [[提示优化]]（Prompt Optimization）与 RL 策略梯度方法（如 [[GRPO]]/[[mmGRPO]]）组合的框架，通过两阶段流水线实现互补收益：先优化提示模板，再微调模型权重。
- **[[DSPy]]**（Declarative Self-improving Python）  
  DSPy (Declarative Self-improving Python) 是一个用于优化 LM 程序的框架，提供声明式编程模型来定义、组合和自动优化多模块 LM 程序。由斯坦福 NLP 团队开发。
- **[[DeepGEMM]]**（DeepGEMM库）  
  DeepSeek 开发的高性能 FP8 GEMM（通用矩阵乘法）内核库，支持 batch-invariant 和 deterministic 计算，是 DeepSeek-V4 训练栈的核心组件。
- **[[DualPipe]]**（双流水线）  
  DeepSeek 提出的双向流水线并行调度策略，通过 1F1B（one-forward-one-backward）交错调度，实现前向和反向计算的高效重叠，最小化流水线气泡。
- **[[Expert Parallelism]]**（EP / 专家并行）  
  一种分布式训练/推理策略，将 MoE 层中的不同专家分布到不同设备上，通过跨设备通信完成 token 的分发（Dispatch）和结果收集（Combine）。
- **[[FP4 Quantization-Aware Training]]**（FP4 QAT / FP4量化感知训练）  
  一种在训练过程中引入 FP4 (MXFP4) 量化模拟的量化感知训练方法，使模型在训练时就适应推理时的低精度量化，减小精度损失。
- **[[Fill-in-Middle]]**（FIM / 填空训练）  
  一种训练数据构造策略，将文本分割为前缀-中间-后缀三段，训练模型根据前缀和后缀预测中间部分，增强模型的代码补全和文本填充能力。
- **[[LoRA]]**（Low-Rank Adaptation / 低秩适配）  
  LoRA（Low-Rank Adaptation）是一种参数高效的微调方法（PEFT），通过向预训练权重注入可训练的低秩分解矩阵来适配下游任务，冻结原始权重。
- **[[Multi-Token Prediction]]**（MTP / 多token预测）  
  一种训练策略，模型在预测下一个 token 的同时也预测后续多个 token，通过增加预测深度来增强模型的表征学习能力。
- **[[Muon Optimizer]]**（Muon / Muon优化器）  
  一种基于 Newton-Schulz 迭代的优化器，通过对梯度矩阵进行近似正交化来更新参数，在大模型训练中展现出比 AdamW 更快的收敛速度和更好的训练稳定性。
- **[[On-Policy Distillation]]**（On-Policy Cross-Stage Distillation / 在线跨阶段蒸馏 / 同策略知识蒸馏）  
  一种将知识蒸馏与策略梯度 RL 融合的训练方法：学生模型在自身生成的 on-policy 轨迹上，将冻结的教师模型的 log-prob 比率直接转化为 RL 目标中的 advantage 信号，实现前序训练能力的跨阶段保留。
- **[[RFT (Rejection Fine-Tuning)]]**（Rejection Fine-Tuning / Rejection Sampling Fine-Tuning / RFT）  
  Rejection Fine-Tuning (RFT)，也称 Rejection Sampling Fine-Tuning，是一种通过采样-过滤-训练循环来改进语言模型的后训练方法。
- **[[SFT]]**（Supervised Fine-Tuning / 监督微调）  
  在预训练基座模型上，使用高质量的人工标注或合成生成的 (prompt, response) 对进行监督学习微调，使模型学会遵循指令格式和在特定领域生成高质量回复。
- **[[Tensor-Level Activation Checkpointing]]**（Tensor-Level AC / 张量级激活检查点）  
  一种细粒度的激活检查点（activation checkpointing / gradient checkpointing）方案，以单个张量而非整个模块为粒度决定保留或重计算，在内存节省和重计算开销之间取得更精细的平衡。
- **[[TileLang]]**（TileLang DSL）  
  一种用于 GPU/NPU 内核开发的领域特定语言（DSL），平衡了开发效率和运行时性能，允许在同一代码库中快速原型和深度迭代优化。
- **[[vLLM]]**（Virtual LLM）  
  一个开源的高性能大语言模型推理引擎，通过 PagedAttention 和连续批处理等技术实现高效 LLM 推理。
- **[[量化]]**（Quantization / Model Quantization / 模型量化）  
  模型量化是将神经网络中的浮点数权重和激活值用低位宽整数（如 INT8、INT4）或二进制表示的压缩技术，旨在减少模型存储和推理时的内存占用与计算开销。

## 🧠 推理与解码

### 搜索算法

- **[[BFS-Prover]]**  
  一种使用广度优先搜索进行形式化定理证明的方法，其核心特点是使用归一化长度的累积对数概率作为节点评分函数。
- **[[Tree-of-Thought]]**（ToT / Tree of Thoughts / 思维树）  
  一种增强 LLM 推理的框架，在推理过程中生成多个思路分支并系统地探索，而非仅沿单一思路链前进。
- **[[UCT]]**（UCB1 / Upper Confidence Bound / Upper Confidence Bound for Trees）  
  UCT（UCB applied to Trees）是 MCTS 中用于节点选择的策略函数，通过上置信界公式在探索与利用之间取得平衡。
- **[[广度优先树搜索]]**（BFTS / Breadth-First Tree Search）  
  广度优先树搜索，一种逐层扩展的树搜索策略，在同一层级的所有节点都被评估后再进入下一层。
- **[[束搜索]]**（BeamSearch / Beam Search）  
  一种受限的宽度优先搜索，在每一层只保留得分最高的 k 个节点（束宽），其余剪枝。
- **[[蒙特卡洛树搜索]]**（MCTS / Monte Carlo Tree Search）  
  一种启发式搜索算法，通过随机模拟（rollout）在巨大的决策空间中逐步构建搜索树，在探索未知分支与利用已知高价值分支之间取得平衡。
### 推理范式

- **[[Best-of-N]]**（BoN / Best-of-N Sampling / Rejection Sampling）  
  最简单的 [[Test-Time Scaling]] 方法：从模型中独立采样 $N$ 个输出，使用奖励模型或验证器（verifier）选择最佳结果。本质上是一种推理时的拒绝采样。
- **[[CoT]]**（Chain-of-Thought / 思维链）  
  Chain-of-Thought（思维链）是一种 prompting 方法，通过在 prompt 中提供逐步推理的示例，引导 LLM 在生成最终答案前先产生中间推理步骤。
- **[[Majority Voting]]**（多数投票 / Self-Consistency / Majority Vote）  
  一种 [[Test-Time Scaling]] 的聚合策略：从模型中独立采样多条推理轨迹，统计最终答案的出现频次，选择最频繁的答案作为输出。也称为 Self-Consistency (Wang et al., 2023)。
- **[[Speculative Decoding]]**（投机解码 / 推测解码）  
  一种加速自回归解码的技术，使用一个轻量级草案模型（draft model）快速生成多个候选 token，然后由目标模型在一次前向传播中并行验证，在不改变输出分布的前提下提升推理速度。
- **[[Test-Time Compute]]**（推理时计算 / Inference-Time Compute / Test-Time Compute Budget）  
  在模型推理阶段可分配的计算资源总量，通常以 FLOPs、生成 token 数或采样轨迹数来衡量。是 [[Test-Time Scaling]] 的核心资源维度。
- **[[Test-Time Scaling]]**（TTS / 测试时缩放 / Test-Time Compute Scaling）  
  在推理阶段通过增加计算量（更多采样、更长推理链、搜索等）来提升模型输出质量的技术范式，与在训练阶段投入计算（Training-Time Scaling）相对。
### 其他

- **[[Compositional Reasoning]]**（组合推理 / Compositional Generalization）  
  Compositional Reasoning（组合推理）指将多个原始技能（primitives）系统地组合成新的、更高层次的推理策略的能力。这是人类智能的标志性特征之一，也是评估 AI 系统泛化能力的关键维度。
- **[[Multi-Temperature Voting]]**（多温度投票 / Two-Stage Voting / Multi-Temperature Voting Algorithm）  
  [[TemperatureScaling]] 论文提出的两阶段投票算法，用于降低多温度 [[Test-Time Scaling|TTS]] 的计算开销。先在每个温度内部投票，再跨温度投票聚合，对"简单"问题提前退出。
- **[[Program-of-Thought]]**（PoT / 程序思维）  
  Program-of-Thought (PoT) 是一种数学推理方法，引导语言模型生成 Python 程序而非自然语言推理步骤，通过执行程序来获得最终答案。
- **[[Temperature Sampling]]**（温度采样 / 温度调节 / Temperature Parameter）  
  通过调整 softmax 函数中的温度参数 $T$ 来控制 LLM token 生成概率分布平滑程度的采样策略。$T \to 0$ 趋于确定性输出（贪心解码），$T$ 增大则增加输出的多样性和随机性。
- **[[Tool-Integrated Reasoning]]**（工具集成推理 / TIR）  
  Tool-Integrated Reasoning（工具集成推理）是一种结合自然语言推理和代码/工具使用的数学推理方法，模型先生成推理步骤，然后用 Python 等工具进行计算验证。
- **[[归一化长度评分]]**（Normalized Lengths Evaluator / 长度归一化评分 / Normalized Log-Probability）  
  对序列的累积对数概率进行长度归一化，防止长序列因 token 累积而获得不公平的低分。
### 形式化方法

- **[[REPL]]**（Read-Eval-Print Loop / 交互式编程环境）  
  一种交互式编程环境，读取用户输入、求值、打印结果、循环。在形式化证明中，REPL 服务器提供实时代码验证和状态查询。
### 工具与框架

- **[[Tensor Core]]**（张量核心 / GPU Tensor Core）  
  NVIDIA GPU 上的专用硬件单元，可在单个时钟周期内执行 $4 \times 4$ 矩阵乘加运算 ($D = A \times B + C$)，是现代深度学习加速的关键硬件。
- **[[llm-reasoners]]**（LLM-Reasoners / InternLM-reasoners）  
  一个用于 LLM 高级推理的库，支持多种推理增强方法，包括 Tree-of-Thought、RAP-MCTS、Guided Decoding 等。

## 💬 提示与上下文学习

- **[[提示优化]]**（Prompt Optimization / 提示工程自动化 / prompt tuning）  
  提示优化 (Prompt Optimization) 是自动搜索和优化 LM 输入提示模板的方法，旨在最大化模型在特定任务上的性能。与模型微调不同，提示优化仅修改输入文本而不改变模型权重。

## 📊 评估与基准

- **[[AIME]]**（AIME 2024 / AIME 2025 / AIME Benchmark）  
  American Invitational Mathematics Examination（美国数学邀请赛），面向高中生的高难度竞赛数学考试。AIME 2024 和 AIME 2025 各含 30 道题，被广泛应用于评估 LLM 的高级数学推理能力。
- **[[Average@N]]**（avg@n / Pass@1 / Average Accuracy）  
  模型在 $N$ 次独立采样中的平均正确率，即 $\text{Avg@}N = C/N$（$C$ 为正确样本数）。等价于单次采样的期望正确率（Pass@1）。
- **[[Hi-ToM]]**（High-order Theory of Mind / 高阶心智理论）  
  High-order Theory of Mind（高阶心智理论）基准，用于评估 LLM 在复杂社会推理场景中对多层嵌套信念和意图的理解能力。包含 100 道题。
- **[[LiveCodeBench]]**（LiveCodeBench v6）  
  一个动态更新的代码生成基准，从 LeetCode、AtCoder 等竞赛平台收集最新编程题目，避免数据污染问题。v6 版本包含 279 道题。
- **[[Llemma]]**  
  Llemma 是一个专注于数学推理的开源语言模型系列（7B 和 34B），基于 Code Llama 继续在 Proof-Pile-2 语料上训练，由 EleutherAI 和 University of Notre Dame 等机构联合发布。
- **[[LongBench]]**（长上下文基准 / LongBench 评测）  
  一个双语、多任务的长上下文理解评测基准，涵盖多个类别的长文本任务（如文档问答、摘要、代码库理解等），按上下文长度分段评估模型性能。
- **[[MATH500]]**  
  MATH 数据集的一个 500 题子集，包含高中竞赛级别的数学问题，广泛用于评估 LLM 的数学推理能力。
- **[[MetaMath]]**  
  MetaMath 是一种通过数据增强提升数学推理能力的方法，对 GSM8K 和 MATH 训练集进行改写和增强，然后在增强数据上微调 Llama-2 模型。
- **[[Minerva]]**  
  Minerva 是 Google 推出的一系列数学推理语言模型（540B/62B/8B），基于 PaLM 架构并在数学文本上继续训练，是早期的数学推理专用模型。
- **[[PAPILLON]]**（Privacy-preserving Delegation）  
  PAPILLON 是一个隐私保护委派任务（Privacy-Preserving Delegation），要求 LM 在处理用户请求时识别并脱敏个人身份信息（PII），同时从知识库中检索相关信息来回答。该任务测试模型在隐私与效用之间平衡的能力。
- **[[Pass@K]]**（pass@k / pass at k）  
  衡量模型在 $K$ 次独立采样中至少产生一个正确结果的概率的评估指标，广泛用于代码生成和数学推理评估。是 [[Test-Time Scaling]] 的标准评估指标。
- **[[RULER]]**（RULER 评测 / 长上下文检索评测）  
  一个综合性长上下文合成评测基准，通过在上下文中插入特定针（needles）并提出相关问题，评估模型从长输入中检索任务相关信息的能力。
- **[[WizardMath]]**  
  WizardMath 是使用 Evol-Instruct 和 PPO 强化学习增强数学推理能力的模型系列，基于 Llama-2 或 Mistral 架构，由 WizardLM 团队开发。
- **[[miniF2F]]**（miniF2F benchmark）  
  一个形式化数学证明基准，包含 488 道来自 IMO、AIME、AMC 等数学竞赛题的形式化版本，广泛用于评估自动化定理证明系统。

---

*自动生成时间: 2026-07-16 11:31 | 共 124 条概念笔记*